import { FC } from 'react';

const RepositoriesList: FC = () => {
    return (
        <div>
            <form>
                <input type="text" />
                <button>Search</button>
            </form>
        </div>
    )
}

export default RepositoriesList;